# Model registry

Models are data records in `config/models.yaml`, not UI constants. Registry entries describe source revision, license, hash, install state, tasks, provider, estimated memory, and safe settings. Estimates are warnings, not guarantees.

Large weights require an explicit install action. The application remains usable when no image model is installed.

