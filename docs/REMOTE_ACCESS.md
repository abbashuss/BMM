# Private remote access

The intended path is phone → Tailscale HTTPS/Serve → FastAPI on loopback. Never publish an engine port. Exact user identity must be allowlisted and verified from trusted proxy metadata only after the production Serve topology is confirmed.

Phase 5 will add tested setup commands and phone reconnect scenarios. No remote-access configuration is active yet.

